local movespeed_power_mult_per_level = %.2f
local function on_movespeed_prop_update(curr_level)
	if curr_level == 0 then
		speed.remove_speed("exp_skill_%s")
		return
	end
	speed.add_speed("exp_skill_%s", 1 + movespeed_power_mult_per_level * curr_level, false, true)
end
