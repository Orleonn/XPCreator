local function on_movespeed_prop_update(curr_level)
	if curr_level == 0 then
		speed.remove_speed("exp_skill_%s")
		return
	end
	speed.add_speed("exp_skill_%s", 1 + %.2f * curr_level, false, true)
end
